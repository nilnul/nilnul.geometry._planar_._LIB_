using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace nilnul.geometry.planar.cloze_
{
	/// <summary>
	/// the inner part of a curve_.closure regardless of the curve's orietation, such that the area is finite but positive&nonZero.
	/// regardless of the translation.
	/// A geometric shape is the geometric information which remains when location, scale, orientation and reflection are removed from the description of a geometric object.[1] That is, the result of moving a shape around, enlarging it, rotating it, or reflecting it in a mirror is the same shape as the original, and not a distinct shape.
	/// In other words, coordds can be changed (shift, scale, rotate, reflect)
	/// </summary>
	public interface ShapeI
	{
	}
}
