namespace nilnul.geometry.planar._troll.locus.to_.fourier_
{
	/// like the len which decomposes light into a spectrum of frequencies in order, which can then pass through an inverted len to compose into the white light again.
	/// <summary>
	/// split the locus to arbitrarily number of rotations (<see cref="nilnul.num.complex_.IPolar"/>, where the azimuth is revolutional speed).
	/// </summary>
	/// 
	public interface IDiscrete : IFourier { }
}
